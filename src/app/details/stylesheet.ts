import { CSSProperties } from "react";
import defaults from "@/constants/default-page-style";

const stylesheet: CSSProperties = {
   ...defaults,
   justifyContent: "space-between",
   alignItems: "center",
}

export default stylesheet;